export class Delivery {
  constructor(version, name, type, size, date, url) {
    this.version = version;
    this.name = name; 
    this.type = type; 
    this.size = size;
    this.date = date; 
    this.url = url;
  }
}